name: Automatyczny Backup Realtime Database

on:
  schedule:
    - cron: '0 0 * * 0' # Co niedzielę o północy
  workflow_dispatch: # Przycisk do ręcznego odpalenia

# Te uprawnienia są KLUCZOWE, aby skrypt mógł zapisać plik w repozytorium
permissions:
  contents: write

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install firebase-admin

      - name: Run Backup Script
        env:
          SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          node -e "
          const admin = require('firebase-admin');
          const fs = require('fs');

          async function run() {
            try {
              const sa = JSON.parse(process.env.SERVICE_ACCOUNT);
              
              // Twój konkretny adres URL w regionie Europa
              const dbUrl = 'https://tenis-rozliczenia-default-rtdb.europe-west1.firebasedatabase.app';
              
              admin.initializeApp({
                credential: admin.credential.cert(sa),
                databaseURL: dbUrl
              });

              console.log('--- Start backup RTDB (Region: Europe) ---');
              
              const db = admin.database();
              const snapshot = await db.ref('/').once('value');
              const data = snapshot.